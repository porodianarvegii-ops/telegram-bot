# Telegram Booking Bot

Бот для записи на услуги с поддержкой расписания, комментариев и отзывов.

## Возможности

- 📅 Выбор даты и времени для записи
- 💬 Добавление комментариев к записи
- ⭐ Система отзывов
- 👤 Админ-панель для управления записями
- 📞 Контакты и портфолио

## Быстрый старт

### Локально

1. Клонируйте репозиторий:
```bash
git clone https://github.com/YOUR_USERNAME/telegram-bot.git
cd telegram-bot
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте `.env` файл:
```bash
cp .env.example .env
```

4. Заполните `.env`:
```
BOT_TOKEN=your_bot_token
ADMIN_IDS=881694689,1076207542
```

5. Запустите:
```bash
python bot.py
```

### На Railway (бесплатно, 24/7)

1. Перейдите на https://railway.app
2. Нажмите `New Project` → `Deploy from GitHub`
3. Выберите этот репозиторий
4. Добавьте переменные окружения в Railway:
   - `BOT_TOKEN` = ваш токен
   - `ADMIN_IDS` = ID администраторов

Готово! Бот будет работать 24/7.

## Команды

- `/start` - Главное меню
- `/admin` - Панель администратора
- `/review` - Оставить отзыв
- `/skip` - Пропустить комментарий

## База данных

SQLite база (bookings.db) содержит:
- `bookings` - записи на услуги
- `reviews` - отзывы пользователей
